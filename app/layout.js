import "./globals.css";

export const metadata = {
  title: "Lagrange Interpolation",
  description: "Pattern interpolation using Lagrange interpolation theorem",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
